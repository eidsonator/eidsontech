app:
    resource: '@AppBundle/Controller/'
    type: annotation

fos_user:
    resource: '@FOSUserBundle/Resources/config/routing/all.xml'

greenskies_log_viewer:
    resource: '@WebLogViewerBundle/Controller'
    type:   annotation
